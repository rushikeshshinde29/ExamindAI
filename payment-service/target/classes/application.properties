# Server Configuration
server.port=5001
server.servlet.context-path=/api

# Database Configuration (Separate database for payment)
spring.datasource.url=jdbc:mysql://localhost:3306/examind_ai_payment?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=cdac
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configurations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configurations (Must match monolith secret to decode)
app.jwt.secret=${JWT_SECRET:9a67479c46d5c6439c3e986a7d5b88219c67479c46d5c6439c3e986a7d5b8821}
app.jwt.expiration-ms=604800000

# Razorpay Keys loaded from environment variables with safe fallback defaults
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_TKvRXea9amopiU}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:YHVFlpZTxf2uw38M2IHhyTqt}

# Monolith Integration
app.monolith.url=http://localhost:5000/api
app.internal.secret=${INTERNAL_API_SECRET:cdac_examind_secret_2026}

# Spring Boot Actuator Configuration
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always

# Mail Configurations
spring.mail.properties.mail.smtp.localhost=localhost
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:team.examind.ai@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ukmwwrvxowwttmxa}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true